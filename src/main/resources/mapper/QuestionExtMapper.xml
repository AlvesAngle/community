<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.hjh.community.mapper.QuestionExtMapper">
  <resultMap id="BaseResultMap" type="life.hjh.community.model.Question">
    <!--
      WARNING - @mbg.generated
      This element is automatically generated by MyBatis Generator, do not modify.
      This element was generated on Tue Mar 03 16:17:39 CST 2020.
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
    <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
    <result column="creator" jdbcType="INTEGER" property="creator" />
    <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    <result column="view_count" jdbcType="INTEGER" property="viewCount" />
    <result column="like_count" jdbcType="INTEGER" property="likeCount" />
    <result column="tag" jdbcType="VARCHAR" property="tag" />
  </resultMap>
  <update id="incView" parameterType="map">
    <!--
     自己写的方法
    -->
    update question
    set  view_count = view_count + #{record.viewCount,jdbcType=INTEGER}
    where id=#{record.id}
  </update>
  <update id="incCommentCount" parameterType="map">
    <!--
     添加评论数目
    -->
    update question
    set  comment_count = comment_count + #{record.commentCount,jdbcType=INTEGER}
    where id=#{record.id}
  </update>
  <select id="selectRelated" parameterType="life.hjh.community.model.Question" resultMap="BaseResultMap">
    select * from QUESTION
    <where>
      <if test="id != null">
        and id != #{id}
      </if>
      <if test="tag != null and tag != ''">
        and tag regexp #{tag}
      </if>
    </where>
    order by gmt_create desc limit 20
  </select>

  <select id="countBySearch" parameterType="life.hjh.community.dto.QuestionQueryDTO" resultType="java.lang.Integer">
    select count(*) from QUESTION
    <where>
      <if test="search != null">
        and title regexp #{search}   /*正则表达式  模糊查询*/
      </if>
    </where>
  </select>

  <select id="selectBySearch" parameterType="life.hjh.community.dto.QuestionQueryDTO" resultMap="BaseResultMap">
    select * from QUESTION
    <where>
      <if test="search != null">
        and title regexp #{search}   /*正则表达式  模糊查询*/
      </if>
    </where>
    order by gmt_create desc
    limit #{page},#{size}
  </select>

  <!-- 获取热门数据 -->

</mapper>